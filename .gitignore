# ----------------------------------------------------------------------
# 1. 编译中间产物 (Compilation Artifacts)
# ----------------------------------------------------------------------
*.o
*.d
*.asm
*.sym
*.map

# ----------------------------------------------------------------------
# 2. 二进制可执行文件与内核 (Binaries & Kernels)
# ----------------------------------------------------------------------
*.elf
*.bin

# 专门针对 kernel 目录下的生成物
kernel/kernel
kernel/kernel.asm
kernel/kernel.sym

# ----------------------------------------------------------------------
# 3. 文件系统与工具 (Filesystem & Tools - 未来会用到)
# ----------------------------------------------------------------------
# xv6 的文件系统镜像
fs.img

# 制作文件系统的工具 (mkfs)
mkfs/mkfs

# ----------------------------------------------------------------------
# 4. 用户态程序 (User Programs - 未来会用到)
# ----------------------------------------------------------------------
# xv6 的 Makefile 通常把编译好的用户程序命名为 _init, _sh 等
user/_*

# ----------------------------------------------------------------------
# 5. 编辑器与 IDE 配置 (Editors & IDEs)
# ----------------------------------------------------------------------
# VS Code
.vscode/
*.code-workspace

# JetBrains (CLion, etc.)
.idea/
cmake-build-*/

# Vim / Emacs 临时文件
*.swp
*~
\#*\#

# ----------------------------------------------------------------------
# 6. 操作系统杂项 (OS Misc)
# ----------------------------------------------------------------------
# Mac OS
.DS_Store

# Windows
Thumbs.db
ehthumbs.db

# ----------------------------------------------------------------------
# 7. 调试与辅助 (Debug & Helpers)
# ----------------------------------------------------------------------
# GDB 调试日志
gdb.txt
.gdbinit

# Clangd / LSP 生成的编译数据库
compile_commands.json
.cache/

# LLM的指导文件
.github/